[client]
# Prevents the app from running custom javascript/iframe restrictions on styles
showSidebarNavigation = true

[ui]
# Forces the sidebar elements to remain structurally visible to the browser layout
sidebarNavVisibility = "visible"

[theme]
# Forces the dashboard to always load with the filter pane open by default
initialSidebarState = "expanded"
